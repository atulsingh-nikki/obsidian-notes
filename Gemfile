source "https://rubygems.org"

# Pin to the same Jekyll and plugin versions that GitHub Pages uses
gem "github-pages", group: :jekyll_plugins

# Required for local Jekyll serve with Ruby 3+
gem "webrick", group: :jekyll_plugins
